{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "description": "Family budget REST API (NestJS + Prisma + PostgreSQL)",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^6.0.3"
  }
}
