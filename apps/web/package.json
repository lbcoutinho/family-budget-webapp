{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Family budget web client (Vite + React + Tailwind)",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
