/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Family Budget API
 * REST API for the family budget web application.
 * OpenAPI spec version: 1.0.0
 */
import type { CashboxesReportMonthDto } from './cashboxesReportMonthDto';

export interface CashboxesReportCashboxDto {
  /**
     * `null` when the cashbox was since deleted (ADR-0019); `name` still carries its label snapshot, and the row is not clickable.
     * @nullable
     */
  cashboxId: string | null;
  name: string;
  /**
     * `null` when `cashboxId` is `null` — a deleted cashbox has no entity left to be active or not.
     * @nullable
     */
  isActive: boolean | null;
  /**
     * Cents. `null` when unset, or when `cashboxId` is `null`.
     * @nullable
     */
  targetAmount: number | null;
  /** Cents, everything confirmed before January of the requested year. */
  openingBalance: number;
  /** Cents, CASHBOX_IN summed over the requested year. */
  deposits: number;
  /** Cents, CASHBOX_OUT summed over the requested year. */
  withdrawals: number;
  /** Cents, CASHBOX_TRANSFER received summed over the requested year. */
  transfersIn: number;
  /** Cents, CASHBOX_TRANSFER sent summed over the requested year. */
  transfersOut: number;
  /** Cents, the running balance at the end of December (or, for a deleted cashbox, at its last active month). */
  closingBalance: number;
  /** Twelve entries, index 0 = January. */
  months: CashboxesReportMonthDto[];
}
