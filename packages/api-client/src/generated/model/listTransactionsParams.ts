/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Family Budget API
 * REST API for the family budget web application.
 * OpenAPI spec version: 1.0.0
 */
import type { TransactionSort } from './transactionSort';
import type { TransactionStatus } from './transactionStatus';
import type { TransactionType } from './transactionType';

export type ListTransactionsParams = {
/**
 * Server-side ordering of the whole filtered set, not just the loaded pages.
 */
sort?: TransactionSort;
/**
 * @minimum 1
 * @maximum 200
 */
limit?: unknown;
/**
 * Opaque to the client — the id of the last row of the previous page.
 */
cursor?: string;
/**
 * Case-insensitive, matches `description` or `notes`.
 */
search?: string;
isCreditCard?: boolean;
/**
 * Matches the live foreign key only — a row whose cashbox was since deleted is excluded (ADR-0019).
 */
cashboxId?: string;
subcategoryId?: string;
categoryId?: string;
/**
 * Matches the source `accountId` only — a TRANSFER shows up under it.
 */
accountId?: string;
/**
 * Omitted ⇒ `CONFIRMED` only (ADR-0012) — balances and reports never see a `DRAFT`.
 */
status?: TransactionStatus;
type?: TransactionType[];
/**
 * Inclusive upper bound on `date`.
 */
dateTo?: string;
/**
 * Inclusive lower bound on `date`.
 */
dateFrom?: string;
/**
 * Normalized to the 1st before matching.
 */
referenceMonth?: string;
};
