/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Family Budget API
 * REST API for the family budget web application.
 * OpenAPI spec version: 1.0.0
 */
import type { CreateInstallmentPlanDtoType } from './createInstallmentPlanDtoType';

export interface CreateInstallmentPlanDto {
  type?: CreateInstallmentPlanDtoType;
  /** The whole purchase, in **cents** (ADR-0005) — not the per-installment amount. */
  totalAmount: number;
  /**
     * 1 installment is a plain transaction, not a plan.
     * @minimum 2
     * @maximum 120
     */
  installments: number;
  /** Expected statement payment date of installment 1. */
  firstPaymentDate: string;
  /** The original purchase date — kept only in each installment’s `notes`. */
  purchaseDate: string;
  /** @maxLength 160 */
  description: string;
  accountId?: string;
  categoryId?: string;
  subcategoryId?: string;
  /** Keeps `referenceMonth` pinned on a later `date` edit (ADR-0009). */
  isCreditCard?: boolean;
  /** Installment amounts are known up front, so this defaults to `true` unlike an open-ended rule. */
  autoConfirm?: boolean;
}
