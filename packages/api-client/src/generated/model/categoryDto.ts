/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Family Budget API
 * REST API for the family budget web application.
 * OpenAPI spec version: 1.0.0
 */
import type { CategoryKind } from './categoryKind';

export interface CategoryDto {
  id: string;
  /**
     * `null` on a root category. A subcategory always points at a root.
     * @nullable
     */
  parentId: string | null;
  name: string;
  /** A subcategory always matches its parent. */
  kind: CategoryKind;
  /**
     * `#RRGGBB`, and set on roots only — subcategories inherit their parent’s colour in the charts. Null is a normal state.
     * @nullable
     */
  color: string | null;
  /** Inactive categories stay readable in history but are kept out of the pickers. */
  isActive: boolean;
  /** Position in the user's own ordering, within its own level. Ties are broken by name. */
  sortOrder: number;
  createdAt: string;
  updatedAt: string;
  /** Subcategories, on roots and only for `?tree=true`. Absent from the flat list, and never present on a subcategory. */
  children?: CategoryDto[];
}
